# Lesson 3: Real-life algorithms as functions
This project shows how to split everyday algorithms into functions,
organize code inside a `utils` package and call them from `main.py`.

## Run

```bash
python3 main.py
```